<div class="main user-show">
  <div class="container">
    <div class="user">
      <img src="/user_images/<%= @user.ref_image_name %>" />
      <h2>
        <%= @user.name %>
      </h2>
      <p>
        <%= @user.email %>
      </p>
      <% if @current_user.id == @user.id %>
      <%= link_to('編集', :edit_user) %>
      <% end %>
    </div>
  </div>
</div>